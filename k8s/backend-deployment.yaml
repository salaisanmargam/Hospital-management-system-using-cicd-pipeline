apiVersion: apps/v1
kind: Deployment
metadata:
  name: medcore-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: medcore-backend
  template:
    metadata:
      labels:
        app: medcore-backend
    spec:
      containers:
      - name: backend
        # Update this tag to the SHA produced by the docker-publish workflow
        image: ghcr.io/salaisanmargam/hospital-management-system-backend:latest
        imagePullPolicy: Always
        envFrom:
        - secretRef:
            name: backend-env
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
