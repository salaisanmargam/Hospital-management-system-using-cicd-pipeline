# Production environment variables (used by Vite during `vite build`)
# On Vercel, the API is served as serverless functions on the same domain,
# so we use a relative /api path — no hardcoded host needed.
VITE_API_URL=/api
VITE_USE_MOCK_AUTH=false
